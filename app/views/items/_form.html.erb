<% form_for_resource do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :container %><br />
    <%= resource.container.code %>
  </p>
  <p>
    <%= f.label :game_base_item %><br />
	<%= lookup(f, :game_base_item_id, GameBaseItem ) %>
  </p>
  <p>
    <%= f.label :code %><br />
    <%= resource.code %>
  </p>
  <p>
    <%= f.label :created_by %><br />
	<%#= collection_select resource, :created_by, skills.find(:all), :id, :name %>
    <%#= f.text_field :created_by %>
  </p>
  <p>
    <%= f.label :visual_model %><br />
	<%= lookup(f, :visual_model_id, GameBaseItem ) %>
  </p>
  <p>
    <%= f.submit 'Save' %>
  </p>
<% end %>
